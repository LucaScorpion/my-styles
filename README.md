# mystyles

Apply custom user styles to pages.

## Development

```shell
# Build and run the extension with Firefox.
npm start

# Build the extension.
npm run build

# Format the code.
npm run format
```
