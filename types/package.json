{
  "name": "types",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "rimraf dist && tsc",
    "lint": "eslint \"src/**/*.ts\" --max-warnings 0",
    "format": "yarn lint --fix"
  },
  "devDependencies": {
    "rimraf": "^3.0.2"
  }
}
