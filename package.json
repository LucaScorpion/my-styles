{
  "name": "my-styles",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "background",
    "web"
  ],
  "scripts": {
    "start": "web-ext run",
    "build": "yarn build:code && yarn build:ext",
    "build:code": "wsrun build",
    "build:ext": "web-ext build",
    "lint": "wsrun lint",
    "format": "wsrun format"
  },
  "devDependencies": {
    "web-ext": "^6.6.0",
    "wsrun": "^5.2.4"
  }
}
